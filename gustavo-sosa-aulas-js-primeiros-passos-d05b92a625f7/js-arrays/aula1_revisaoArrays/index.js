/*const nomes = ['Eduardo', 'Maria', 'Joana'];
const nomes = new Array('Eduardo', 'Maria', 'Joana');
nomes[2] = 'joao';
delete nomes[2]; //deleta valor do array sem deletar seu indice
const novo = [...nomes]; //assim o que eu faço em novo, NÃO reflete em nomes

const removido = novo.pop(); // atribuo a variavel removido o valor que foi excluido do array novo

alunos.push('Hernandez');
alunos.push('Eduardo');

console.log();

delete alunos [1]; // remove o elemento do array e deixa seu indice em branco
alunos.pop(); // remove o ultimo elemento do array removendo tbm o indice
alunos.shift(); // remove o primeiro elemento do array removendo tbm o indice
alunos.unshift('Mariana'); // lançar dados ao inicio do array
alunos.push('Hernandez'); // lançar dados ao final do array
alunos[0] = 'Eduardo';
alunos[alunos.length] = 'Gustavo'; //adiciona elemento ao final do array
alunos[3] = 'Luiza'; //adiciona um elemento a um indice especifico do array
console.log(alunos.slice(0, 3)); // fatiar exibição
console.log(alunos instanceof Array);// verificar se a variavel é um array

console.log(nomes.length);
console.log(novo, removido, nomes);

 const nome = 'Gustavo sosa teste';
 const nomes = nome.split(' '); // transforma uma string em um array com espaço nesse exemplo
 console.log(nomes);
 */
 const nomes = [ 'Gustavo', 'sosa', 'teste' ];
 const nome = nomes.join(' '); //transforma um array em uma string por espaço nesse exemplo, podendo ser por virgula ou o que eu colocasse dentro do join
 console.log(nome);
